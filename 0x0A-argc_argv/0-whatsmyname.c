#include "main.h"
#include <stdio.h>

/**
 * main  - To print a name, followed by a new line
 * @arg_count: to count the number of arg
 * @arg_vector: to print the name of arg
 * Return: 0
 */

int main(int arg_count __attribute__ ((unused)), char *arg_vector[])
{
	printf("%s\n", arg_vector[0]);
	printf("%s\n", arg_vector[0]);

	return (0);
}
